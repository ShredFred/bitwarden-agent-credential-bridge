node_modules/
.DS_Store
*.log
.env
.env.*
.agent-broker/
/reports/review/
# Phase 13 personal vault allowlist must stay machine-local (digest only).
personal-vault.allow.json
**/personal-vault.allow.json
# Phase 14 Secrets Manager machine allowlist + local token artifacts.
sm-machine.allow.json
**/sm-machine.allow.json
sm-machine.token
**/sm-machine.token
# Installer build output
/dist/
# Agent Playwright CLI session artifacts (not the Bridge-owned browser).
.playwright-cli/
